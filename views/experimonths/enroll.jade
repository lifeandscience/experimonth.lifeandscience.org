extends ../layout

block body
	#container-inner
		h1 #{title}
		h4 Begins #{locals.moment(experimonth.startDate).utc().format('MMMM D, YYYY')} and ends #{locals.moment(experimonth.endDate).utc().format('MMMM D, YYYY')}.

		p Donate your anonymized experimonth data to scientists:
		p !{experimonth.survey}
		
		form(action="/experimonths/enroll/"+experimonth._id, method="POST")
			.form-actions
				button.btn.btn-primary(type="submit", name="submit", value="Accept") Accept
				|  
				button.btn(type="submit", name="submit", value="Decline") Decline
				|  You can join the experimonth regardless of your choice.
extends ../layout

include ./mixins

block body
	#container-inner
		#header
			h1 Confess
			
		.row
			.span8
				p 
					| Whatever you say here is completely anonymous and stripped of identifying information before it’s sent to the Experimonth team.  It may appear on our 
					a(href="/confessional") Confessions page
					| .

				form(action=action, method="POST")#confessionForm.form-horizontal
					textarea#text.input-block-level(type="text", placeholder="", name="text", rows="15") !{confession.text}
					input(type="hidden", name="timestamp", value=locals.timestamp)
					input(type="hidden", name="nonce", value=locals.nonce)
					label.checkbox
						input.checkbox(type="checkbox", name="whatareyou", value="hewmahn")
						| Are you a human? If so, check this box.
					button.btn.btn-inverse.pull-right(type="submit") confess

			.span4
				if random_confession
					div.confession.white-card
						mixin do_confession(random_confession, true)
				div.view-all-confessions.text-center
					a(href="/confessional") View all confessions.